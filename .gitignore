# Node
node_modules/
dist/
build/
*.tsbuildinfo

# Окружение и секреты
.env
.env.*
!.env.example

# Артефакты запуска дней
day-07-history.json

# Runtime-данные (.data) — НЕ в git: SQLite, memory.json, profiles, MTProto-сессия.
challenge/.data/

# Web (день 28) — локальный Next.js. Артефакты dev-компиляции и локальный env.
web/.next/
web/.env.local
web/node_modules/

# Рабочие артефакты оркестратора (research/plan/fix/validation/report + run-logs) —
# не продукт, персистентный канал между субагентами. Аудит-след живёт локально.
swarm-report/

# Visual companion (superpowers brainstorm) — мокапы/стейт, не продукт
.superpowers/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Playwright MCP runtime artifacts
.playwright-mcp/
