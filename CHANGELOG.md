# Changelog

Хронология челленджа. Запись = полный текст задания + что сделано + выводы + код.

---

## День 1 — Первый запрос к LLM через API
**Дата:** 2026-06-01

**Задание (полный текст):**
> Минимальный код:
> - запрос в LLM через API
> - получить ответ
> - вывести в консоль/простой интерфейс (CLI / Web)
>
> Результат: код — запрос в LLM через API + ответ.
> Формат: видео + код.

**Сделано:**
- Архив (`1-day/index.ts`): фрагмент TS-клиента в Next.js-приложении.
- TS-порт (`challenge/src/demos/day-01.ts`): автономный скрипт через `@challenge/core::LlmClient`.

**Выводы:**
- OpenAI-совместимый `/chat/completions` одинаков для DeepSeek и OpenRouter — разница только base_url и ключ.
- Минимальный запрос: `{model, messages}`.

**Код:** `challenge/src/demos/day-01.ts`

---

## День 2 — Формат ответа
**Дата:** 2026-06-02

**Задание (полный текст):**
> Тот же запрос, но:
> - явное описание формата ответа
> - ограничение длины ответа
> - условие завершения (stop sequence или явная инструкция)
>
> Сравнить: без ограничений vs с ограничениями.
> Результат: тот же запрос с разным уровнем контроля через API.
> Формат: видео + код.

**Сделано:**
- Без ограничений, JSON-формат, stop-sequence + `max_tokens=60`.

**Выводы:**
- Явное описание формата в промпте надёжнее параметра `stop`.
- `max_tokens` пригодится для дня 8 (токены), но жёсткий лимит режет ответ посередине мысли.

**Код:** `challenge/src/demos/day-02.ts`

---

## День 3 — Разные способы рассуждения
**Дата:** 2026-06-05

**Задание (полный текст):**
> Одна задача (логическая/алгоритмическая/аналитическая). Решить через API 4 способами:
> 1. прямой ответ без доп. инструкций
> 2. инструкция «решай пошагово»
> 3. модель сначала составляет промпт для задачи, потом используем его
> 4. группа экспертов (аналитик, инженер, критик) — решение от каждого
>
> Сравнить: отличаются ли ответы, какой способ точнее.
> Результат: несколько решений + сравнение.
> Формат: видео + код.

**Сделано:**
- Прямой ответ, "думай пошагово", мета-промпт, группа экспертов.

**Выводы:**
- "Пошагово" = самый структурированный и проверяемый ответ.
- Мета-промпт полезен для размытых задач — модель сама уточняет себя.

**Код:** `challenge/src/demos/day-03.ts`

---

## День 4 — Температура
**Дата:** 2026-06-07

**Задание (полный текст):**
> Один запрос с параметрами:
> - temperature = 0
> - temperature = 0.7
> - temperature = 1.2
>
> Сравнить: точность, креативность, разнообразие.
> Сформулировать: какие задачи лучше для каждой настройки.
> Результат: примеры ответов с разной температурой + выводы.
> Формат: видео + код.

**Сделано:**
- Три прогона, выводим рядом.

**Выводы:**
- 0 — фактические ответы и код.
- 0.7 — слоганы, краткие тексты, баланс.
- 1.2 — брейншторм, диверсификация идей; иногда рвёт логику.

**Код:** `challenge/src/demos/day-04.ts`

---

## День 5 — Версии моделей
**Дата:** 2026-06-07

**Задание (полный текст):**
> Один запрос:
> - слабая модель
> - средняя модель
> - сильная модель
> (начало/середина/конец списка HuggingFace)
>
> Замерить: время ответа, кол-во токенов, стоимость (если платная).
> Сравнить: качество, скорость, ресурсоёмкость.
> Результат: короткий вывод о различиях + ссылки.
> Формат: видео + код.

**Сделано:**
- OpenRouter: `gpt-4o-mini`, `gemini-2.0-flash`, `claude-3.5-sonnet`.
- Замеряем время и токены из `usage`.

**Выводы:**
- Слабые модели быстрее/дешевле, но путаются в многошаговых рассуждениях.
- На длинных промптах расход токенов растёт нелинейно — важно для дня 8.

**Код:** `challenge/src/demos/day-05.ts`

---

## День 6 — Первый агент
**Дата:** 2026-06-09

**Задание (полный текст):**
> Простой агент:
> - принимает запрос пользователя
> - отправляет в LLM через API
> - получает ответ
> - выводит результат в интерфейсе
> (чат/CLI/web, запросы через HTTP-клиент)
>
> Важно:
> - агент = отдельная сущность, не один вызов API
> - логика запроса/ответа инкапсулирована в агенте
>
> Результат: агент принимает запрос и корректно вызывает LLM через API.
> Формат: видео + код.

**Сделано:**
- `class Agent` с методом `say`, хранит system + history.
- REPL через `node:readline/promises`: ввод пользователя -> LLM -> ответ.

**Выводы:**
- Логика запроса инкапсулирована в агенте, UI не знает про HTTP.
- Этот агент — основа всех последующих дней (контекст, токены, ветки).

**Код:** `challenge/src/demos/day-06.ts`

---

## День 7 — Сохранение контекста
**Дата:** 2026-06-10

**Задание (полный текст):**
> Добавить агенту сохранение контекста:
> - история диалога (messages) в JSON или SQLite
> - при перезапуске загружать историю обратно
> - продолжать диалог будто агент не выключался
>
> Проверить:
> - начать диалог
> - перезапустить приложение
> - продолжить диалог, убедиться что агент помнит прошлое
>
> Результат: агент сохраняет и восстанавливает контекст между запусками.
> Формат: видео + код.

**Сделано:**
- `day-07-history.json` рядом с бинарником.
- `loadHistory` + `saveHistory` через `node:fs/promises` вокруг каждого хода.

**Выводы:**
- JSON годится для демо; проде — SQLite (см. день 9 — compression).
- Восстановление работает только если системный промпт детерминирован.

**Код:** `challenge/src/demos/day-07.ts`

---

## День 8 — Работа с токенами
**Дата:** 2026-06-11

**Задание (полный текст):**
> Подсчёт токенов в коде агента:
> - текущий запрос
> - вся история диалога
> - ответ модели
>
> Сравнить:
> - короткий диалог
> - длинный диалог
> - диалог, превышающий лимит модели
>
> Показать: рост стоимости/токенов по ходу диалога, что ломается при переполнении.
>
> Результат: код считает токены и показывает влияние на поведение.
> Формат: видео + код.

**Сделано:**
- Поле `usage` из ответа API.
- Копим `cum: Usage` по ходам.

**Выводы:**
- На длинной истории `prompt_tokens` растёт линейно — главный аргумент за compression (день 9) и windowing (день 10).
- Без локального tokenization точный подсчёт невозможен — `usage` единственный честный источник.

**Код:** `challenge/src/demos/day-08.ts`

---

## День 9 — Управление контекстом: сжатие истории
**Дата:** 2026-06-11

**Задание (полный текст):**
> Механизм управления контекстом:
> - последние N сообщений «как есть»
> - остальное — summary (например каждые 10 сообщений)
> - summary отдельно, подставлять в запрос вместо полной истории
>
> Сравнить:
> - качество без сжатия
> - качество со сжатием
> - расход токенов до/после
>
> Результат: агент с компрессией истории, экономит токены.
> Формат: видео + код.

**Сделано:**
- `summarize()` отдельным вызовом LLM.
- Пересоздаём summary каждые K сообщений, подставляем как `system`.

**Выводы:**
- Экономия на `prompt_tokens` видна сразу после первой компрессии.
- Качество слегка падает, если summary слишком агрессивно сжало факты — мотивация для Sticky Facts (день 10).

**Код:** `challenge/src/demos/day-09.ts`

---

## День 10 — Управление контекстом: разные стратегии (без summary)
**Дата:** 2026-06-14

**Задание (полный текст):**
> 3 стратегии управления контекстом (минимум) + переключатель:
>
> Стратегия 1: Sliding Window
> - только последние N сообщений
> - остальное отбрасывать
>
> Стратегия 2: Sticky Facts / Key-Value Memory
> - блок "facts" (ключ-значение) — важные данные из диалога (цель, ограничения, предпочтения, решения, договорённости)
> - обновлять facts после каждого сообщения пользователя
> - в запрос: facts + последние N сообщений
>
> Стратегия 3: Branching (ветки диалога)
> - checkpoint в диалоге
> - 2 ветки от одного места
> - продолжить диалог в каждой ветке независимо
> - переключаться между ветками
>
> Тест на одном сценарии (например "собираем ТЗ 10–15 сообщений"): прогнать каждую стратегию, сравнить ответы и поведение.
>
> Сравнить: качество ответа, стабильность (не теряет ли детали), расход токенов, удобство.
>
> Результат: агент с 3 стратегиями (Sliding Window / Facts / Branching) + сравнение.
> Формат: видео + код.

**Сделано:**
- Три класса реализуют `ContextStrategy` из `@challenge/core`.
- Демо: один сценарий (сбор ТЗ) на каждой стратегии.
- Branching: `checkpoint(label)` + `switchTo(id)`.

**Выводы:**
- Sliding — самый дешёвый, но теряет факты.
- Sticky Facts — лучший баланс при адекватных правилах извлечения.
- Branching незаменим для "что если" и A/B тестинга промптов.

**Код:** `challenge/src/demos/day-10.ts`

---

## День 11 — <заглушка>
**Дата:** TBD

**Задание:**
> TBD

**Сделано:**
- (заполняется при получении задания)

**Выводы:**
- TBD

**Код:** `challenge/src/demos/day-11.ts` (ветка `day-11`)

---

## Feature: блог-агенты для канала «Иди на факты глянь»
**Дата:** 2026-06-17
**Ветка:** `feature/blog-agents`

**Цель:** автоматизировать ежедневную подготовку постов для футбольного Telegram-канала. Три агента в pipeline: сбор новостей → написание поста в авторском стиле → фактчекинг.

**Что сделано:**
- Хранилище: SQLite через `node:sqlite` (Node 24+, без нативных сборок). Таблицы `news`, `posts`, `style_samples` с UNIQUE-индексами против дублей.
- RSS-загрузчик: sports.ru, championat.com, BBC Sport (через `fast-xml-parser`).
- Агент 1 (NewsFetcher): LLM-выбор top-K хайповых из неиспользованных.
- Агент 2 (PostWriter): few-shot на 5 случайных образцах стиля канала.
- Агент 3 (FactChecker): JSON-verdict, сверка фактов с источником.
- CLI: `news`, `seed-style`, `db-stats` с флагами `--hours`, `--top`, `--for`.
- Документация: раздел про хранение контекста, cron для Windows/Linux.

**Параметры:**
- Провайдер: DeepSeek (через `.env`).
- Источник стиля: 13 реальных постов канала (через `seed-style`).
- Хранилище: `challenge/.data/blog.sqlite` (в `.gitignore`).

**Smoke-результат** (реальный прогон):
- Агент 1: топ-3 (Роналду-ЧМ, Скалони-Месси, Англия-Хорватия).
- Агент 2: пост про Скалони, 219 символов, с шапкой/CAPS/иронией/подписью.
- Агент 3: `verdict: ok`, «Пост полностью соответствует источнику».

**Выводы:**
- `node:sqlite` годится для локальных cron-агентов без боли с нативными сборками (`better-sqlite3` требует ClangCL toolchain на Windows).
- Строгий JSON-промпт требует: явный пример, требование «первый символ ответа — `[`», `maxTokens >= 2000` для больших промптов, толерантный парсер (устойчив к markdown-обёртке и обрыву).
- Few-shot на 5 случайных образцов стиля даёт удовлетворительный результат; для проде расширить до 10-15 и/или кластеризовать по типам постов.

**Код:**
- `challenge/src/core/db.ts` — слой SQLite.
- `challenge/src/core/agents/{rss,newsFetcher,postWriter,factChecker,pipeline,seed}.ts`
- `challenge/src/data/style-samples.json` — 13 образцов стиля.
- Команды в `challenge/src/cli.ts`: `news`, `seed-style`, `db-stats`.

---

## День 16 — MCP-клиент: соединение и список инструментов
**Дата:** 2026-06-24

**Задание (полный текст):**
> В рамках шестнадцатого дня необходимо написать код общения с МСП Claude In
> Mobile. Сделайте минимальный код, который:
> - устанавливает MCP-соединение
> - получает от MCP список доступных инструментов

**Сделано:**
- `core/mcp.ts` — минимальный MCP-клиент: JSON-RPC 2.0 поверх stdio дочернего
  процесса. Проводит handshake (`initialize` → `notifications/initialized`),
  затем вызывает `tools/list` (и умеет `tools/call`).
- `demos/day-16.ts` — сценарий: запускает `claude-in-mobile`, печатает
  `serverInfo` и список инструментов.

**Выводы:**
- MCP — это JSON-RPC 2.0. Транспорт stdio: клиент пишет запросы в stdin
  дочернего процесса, читает ответы из stdout, по одной строке на сообщение.
- Жизненный цикл строго трёхфазный: `initialize` (обмен версиями протокола и
  capabilities) → `notifications/initialized` (клиент готов) → обычные запросы.
  Без `initialize` сервер не отвечает на `tools/list`.
- Версия протокола согласуется в `initialize`: клиент шлёт `2025-06-18`, сервер
  подтверждает ту же. Несовпадение — повод разорвать соединение.
- claude-in-mobile v3.14.0 (profile `core`) отдаёт 14 инструментов: 7
  мета-инструментов (`device`, `screen`, `input`, `ui`, `app`, `system`,
  `flow`) + 7 REPL-инструментов (`repl_spawn`, `repl_send`, `repl_key`,
  `repl_expect`, `repl_snapshot`, `repl_list`, `repl_kill`). Опциональные
  модули (`browser`, `desktop`, `store`) подгружаются через
  `device(enable_module)`.

**Smoke-результат** (реальный прогон):
```
protocol: 2025-06-18
server:   claude-in-mobile v3.14.0
Инструментов доступно: 14
```

**Код:**
- `challenge/src/core/mcp.ts` — `McpStdioClient`: connect/handshake/listTools/callTool/disconnect.
- `challenge/src/demos/day-16.ts` — демо: запуск сервера + печать инструментов.

---

## День 17 — Первый инструмент MCP: standalone HTTP-сервер + агент-клиент
**Дата:** 2026-06-24

**Задание (полный текст):**
> Реализуйте свой MCP-сервер вокруг любого API. Сделайте:
> - регистрацию инструмента
> - описание входных параметров
> - возврат результата
>
> Подключите инструмент к своему агенту и:
> - вызовите его из приложения
> - получите и используйте результат
>
> Результат: Агент делает вызов к MCP-инструменту и получает результат.

**Сделано:**
- `core/mcpHttpServer.ts` — переиспользуемый MCP-сервер поверх HTTP
  (Streamable HTTP transport): `McpHttpServer` на чистом `node:http`, без
  зависимостей. Методы: `initialize`, `notifications/initialized`, `tools/list`,
  `tools/call`. GET — статус-страница, OPTIONS — CORS, POST — JSON-RPC.
- `core/mcpHttpClient.ts` — MCP-клиент поверх HTTP: `McpHttpClient` на встроенном
  `fetch`. Методы: `connect` (handshake), `listTools`, `callTool`, `disconnect`.
- `demos/day-17-server.ts` — standalone MCP HTTP-сервер `challenge-mcp-server`
  с 4 инструментами: `get_user_posts`, `get_todos` (JSONPlaceholder API),
  `add_note`, `list_notes` (локальный in-memory стор).
- `demos/day-17.ts` — демо: агент подключается по HTTP к работающему серверу,
  через текстовый протокол tool-calling вызывает инструменты, формулирует ответ.
- CLI: команда `mcp-server [--port N]` запускает сервер в отдельном терминале.

**Архитектура:**
```
Терминал 1:                          Терминал 2:
┌─────────────────────────┐          ┌─────────────────────────┐
│  MCP HTTP Server        │          │  Day-17 Demo (Agent)    │
│  pnpm start -- mcp-server│         │  pnpm start -- day-17   │
│  port :3001/mcp         │◄────────►│  HTTP MCP Client        │
│                         │  HTTP    │                         │
│  Tools:                 │  JSON-   │  1. connect (initialize)│
│  • get_user_posts       │  RPC     │  2. listTools           │
│  • get_todos            │  2.0     │  3. agent → CALL tool   │
│  • add_note             │          │  4. callTool → result   │
│  • list_notes           │          │  5. agent final answer  │
└─────────────────────────┘          └─────────────────────────┘
```

**Выводы:**
- Standalone HTTP MCP-сервер ближе к продакшену, чем stdio child-process.
  Сервер живёт отдельно, слушает порт, отвечает на HTTP POST с JSON-RPC.
  Так работают публичные MCP-серверы (mcp.inevitable.fyi и др.).
- Транспорт Streamable HTTP: каждый JSON-RPC вызов — отдельный POST на `/mcp`.
  Заголовок `MCP-Protocol-Version: 2025-06-18` обязателен. Уведомления (без id) →
  HTTP 202. Запросы (с id) → JSON-RPC response в теле.
- Текстовый протокол tool-calling (`CALL: name {json}` / `RESULT: text`) работает
  надёжнее нативного `tools`-параметра. Агент узнаёт о всех инструментах из
  system-промпта, сам выбирает нужный, парсит ответ регэкспом.
- Разделение сервер/клиент = как у настоящих MCP: Claude Desktop подключается к
  серверам, агент из демо подключается к нашему серверу. Сервер можно поднять на
  VPS, поменять только URL — агент продолжит работать.

**Smoke-результат** (2 терминала):
```
# Терминал 1
$ pnpm --filter challenge start -- mcp-server
▶ MCP HTTP-сервер: старт на http://localhost:3001/mcp
MCP server 'challenge-mcp-server' listening on http://localhost:3001/mcp

# Терминал 2
$ pnpm --filter challenge start -- day-17
✓ Сервер: challenge-mcp-server v1.0.0 (протокол 2025-06-18)
✓ Инструментов: 4
--- Сценарий 1 ---
[итерация 1] LLM → CALL: get_user_posts {"userId":2}
  → MCP-вызов... Found 10 post(s) for user 2.
[итерация 2] LLM → финальный ответ: 10 постов
--- Сценарий 2 ---
[итерация 1] LLM → CALL: add_note {"text":"Изучить MCP протокол"}
  → MCP-вызов... Note added with id 1
[итерация 2] LLM → CALL: list_notes {}
  → MCP-вызов... 1. [#1] Изучить MCP протокол
[итерация 3] LLM → финальный ответ: заметка добавлена
```

**Код:**
- `challenge/src/core/mcpHttpServer.ts` — `McpHttpServer`: HTTP, node:http, 0 deps.
- `challenge/src/core/mcpHttpClient.ts` — `McpHttpClient`: HTTP, built-in fetch.
- `challenge/src/demos/day-17-server.ts` — standalone сервер: 4 инструмента + JSONPlaceholder.
- `challenge/src/demos/day-17.ts` — демо: агент-клиент, 2 сценария, tool-calling loop.
- `challenge/src/cli.ts` — команда `mcp-server [--port N]`.