# Changelog

Хронология прохождения челленджа. Каждая запись содержит: полный текст задания,
что сделано, какие выводы, где посмотреть код.

---

## День 1 — Первый запрос к LLM через API
**Дата:** 2026-06-01

**Задание (полный текст):**
> Напишите минимальный код, который:
> - отправляет запрос в LLM через API
> - получает ответ
> - выводит его в консоль или простой интерфейс (CLI / Web)
>
> Результат: код, который отправляет запрос в LLM через API и получает ответ.
> Формат: видео + код.

**Сделано:**
- В архиве (`1-day/index.ts`): фрагмент TS-клиента в контексте Next.js-приложения.
- TS-порт (`challenge/days/day-01-first-request/`): автономный скрипт через
  `@challenge/core::LlmClient`.

**Выводы:**
- OpenAI-совместимый `/chat/completions` работает одинаково и для DeepSeek,
  и для OpenRouter — отличается только base_url и ключ.
- Минимальный запрос: `{model, messages}`.

**Код:** `challenge/days/day-01-first-request/index.ts`

---

## День 2 — Формат ответа
**Дата:** 2026-06-02

**Задание (полный текст):**
> Отправьте один и тот же запрос, но:
> - добавьте явное описание формата ответа
> - добавьте ограничение на длину ответа
> - добавьте условие завершения ответа (stop sequence или явную инструкцию)
>
> Сравните ответы: без ограничений vs с ограничениями.
> Результат: один и тот же запрос с разным уровнем контроля ответа через API.
> Формат: видео + код.

**Сделано:**
- Без ограничений, JSON-формат, stop-sequence + `max_tokens=60`.

**Выводы:**
- Явное описание формата в промпте надёжнее, чем параметр `stop`.
- `max_tokens` пригодится для дня 8 (токены), но жесткий лимит обрезает ответ
  посередине мысли.

**Код:** `challenge/days/day-02-format/index.ts`

---

## День 3 — Разные способы рассуждения
**Дата:** 2026-06-05

**Задание (полный текст):**
> Возьмите одну задачу (логическую, алгоритмическую или аналитическую).
> Решите её через API четырьмя способами:
> 1. получите прямой ответ без дополнительных инструкций
> 2. добавьте в промпт инструкцию: «решай пошагово»
> 3. попросите модель сначала составить промпт для решения задачи, а затем
>    используйте его
> 4. создайте в промпте группу экспертов (например: аналитик, инженер, критик)
>    и получите решение от каждого
>
> Сравните: отличаются ли ответы, какой способ дал наиболее точный результат.
> Результат: несколько решений одной задачи и их сравнение.
> Формат: видео + код.

**Сделано:**
- Прямой ответ, "думай пошагово", мета-промпт, группа экспертов.

**Выводы:**
- "Пошагово" даёт самый структурированный и проверяемый ответ.
- Мета-промпт полезен, когда задача размытая — модель сама уточняет себя.

**Код:** `challenge/days/day-03-reasoning/index.ts`

---

## День 4 — Температура
**Дата:** 2026-06-07

**Задание (полный текст):**
> Выполните один и тот же запрос с параметрами:
> - temperature = 0
> - temperature = 0.7
> - temperature = 1.2
>
> Сравните ответы по: точности, креативности, разнообразию.
> Сформулируйте: для каких задач лучше подходит каждая настройка.
> Результат: примеры ответов с разной температурой и выводы по их использованию.
> Формат: видео + код.

**Сделано:**
- Три прогона, выводим рядом.

**Выводы:**
- 0 — для фактических ответов и кода.
- 0.7 — слоганы, краткие тексты, баланс.
- 1.2 — брейншторм, диверсификация идей; иногда рвёт логику.

**Код:** `challenge/days/day-04-temperature/index.ts`

---

## День 5 — Версии моделей
**Дата:** 2026-06-07

**Задание (полный текст):**
> Выполните один и тот же запрос:
> - на слабой модели
> - на средней модели
> - на сильной модели
> (например: из начала, середины и конца списка HuggingFace)
>
> Замерьте: время ответа, количество токенов, стоимость (если модель платная).
> Сравните: качество ответов, скорость, ресурсоёмкость.
> Результат: короткий вывод о различиях между моделями + ссылки.
> Формат: видео + код.

**Сделано:**
- На OpenRouter: `gpt-4o-mini`, `gemini-2.0-flash`, `claude-3.5-sonnet`.
- Замеряем время и токены из `usage`.

**Выводы:**
- Слабые модели быстрее и дешевле, но путаются в многошаговых рассуждениях.
- На длинных промптах расход токенов растёт нелинейно — важно для дня 8.

**Код:** `challenge/days/day-05-models/index.ts`

---

## День 6 — Первый агент
**Дата:** 2026-06-09

**Задание (полный текст):**
> Реализуйте простого агента, который:
> - принимает запрос пользователя
> - отправляет его в LLM через API
> - получает ответ
> - выводит результат в вашем интерфейсе
> (простой чат, CLI или web, запросы через HTTP-клиент)
>
> Важно:
> - агент должен быть отдельной сущностью, а не просто один вызов API
> - логика запроса и ответа должна быть инкапсулирована в агенте
>
> Результат: агент принимает запрос и корректно вызывает LLM через API.
> Формат: видео + код.

**Сделано:**
- `class Agent` с методом `say`, хранит system + history.
- REPL через `node:readline/promises`: ввод пользователя -> LLM -> ответ.

**Выводы:**
- Логика запроса инкапсулирована в агенте, UI ничего не знает про HTTP.
- Этот агент — основа для всех последующих дней (контекст, токены, ветки).

**Код:** `challenge/days/day-06-agent/index.ts`

---

## День 7 — Сохранение контекста
**Дата:** 2026-06-10

**Задание (полный текст):**
> Добавьте агенту сохранение контекста:
> - храните историю диалога (messages) в JSON или SQLite
> - при перезапуске агента загружайте историю обратно
> - продолжайте диалог так, как будто агент не выключался
>
> Проверьте на практике:
> - начните диалог
> - перезапустите приложение
> - продолжите диалог и убедитесь, что агент помнит прошлые сообщения
>
> Результат: агент, который сохраняет и восстанавливает контекст между запусками.
> Формат: видео + код.

**Сделано:**
- `day-07-history.json` рядом с бинарником.
- `loadHistory` + `saveHistory` через `node:fs/promises` вокруг каждого хода.

**Выводы:**
- JSON хорош для демо; на проде нужен SQLite (см. день 9 — compression).
- Восстановление работает, только если системный промпт детерминирован.

**Код:** `challenge/days/day-07-persistence/index.ts`

---

## День 8 — Работа с токенами
**Дата:** 2026-06-11

**Задание (полный текст):**
> Добавьте в код агента подсчёт токенов:
> - для текущего запроса
> - для всей истории диалога
> - для ответа модели
>
> Сравните:
> - короткий диалог
> - длинный диалог
> - диалог, который превышает лимит модели
>
> Покажите: как растёт стоимость/токены по мере диалога, что ломается при
> переполнении.
>
> Результат: код, который считает токены и показывает, как они влияют на поведение.
> Формат: видео + код.

**Сделано:**
- Используем поле `usage` из ответа API.
- Копим `cum: Usage` по ходам.

**Выводы:**
- На длинной истории `prompt_tokens` растёт линейно — это главный аргумент
  за compression (день 9) и windowing (день 10).
- Без локального tokenization точный подсчёт невозможен — `usage` единственный
  честный источник.

**Код:** `challenge/days/day-08-tokens/index.ts`

---

## День 9 — Управление контекстом: сжатие истории
**Дата:** 2026-06-11

**Задание (полный текст):**
> Реализуйте механизм управления контекстом:
> - храните последние N сообщений «как есть»
> - остальное заменяйте summary (например каждые 10 сообщений)
> - храните summary отдельно и подставляйте его в запрос вместо полной истории
>
> Сравните:
> - качество ответов без сжатия
> - качество ответов со сжатием
> - расход токенов до/после
>
> Результат: агент, который работает с компрессией истории и экономит токены.
> Формат: видео + код.

**Сделано:**
- `summarize()` отдельным вызовом LLM.
- Пересоздаём summary каждые K сообщений, подставляем как `system`.

**Выводы:**
- Экономия на `prompt_tokens` видна сразу после первой компрессии.
- Качество ответа слегка падает, если summary слишком агрессивно сжало факты —
  отсюда мотивация для Sticky Facts (день 10).

**Код:** `challenge/days/day-09-compression/index.ts`

---

## День 10 — Управление контекстом: разные стратегии (без summary)
**Дата:** 2026-06-14

**Задание (полный текст):**
> Реализуйте в агенте 3 разных стратегии управления контекстом (минимум),
> и переключатель между ними:
>
> Стратегия 1: Sliding Window
> - храните только последние N сообщений
> - всё остальное отбрасывайте
>
> Стратегия 2: Sticky Facts / Key-Value Memory
> - введите отдельный блок "facts" (ключ-значение), который хранит важные данные
>   из диалога (например: цель, ограничения, предпочтения, решения, договорённости)
> - обновляйте facts после каждого сообщения пользователя
> - в запрос отправляйте: facts + последние N сообщений
>
> Стратегия 3: Branching (ветки диалога)
> - сохраните checkpoint в диалоге
> - создайте 2 ветки от одного места
> - продолжите диалог в каждой ветке независимо
> - переключайтесь между ветками
>
> Протестируйте на одном и том же сценарии (например: "собираем ТЗ 10–15
> сообщений"): прогоните сценарий на каждой стратегии, сравните ответы и
> поведение.
>
> Сравните: качество ответа, стабильность (не теряет ли важные детали),
> расход токенов, удобство для пользователя.
>
> Результат: агент с 3 стратегиями управления контекстом
> (Sliding Window / Facts / Branching) + сравнение результатов.
> Формат: видео + код.

**Сделано:**
- Три класса, реализующих `ContextStrategy` из `@challenge/core`.
- Демо с одним и тем же сценарием (сбор ТЗ) на каждой стратегии.
- Branching: `checkpoint(label)` + `switchTo(id)`.

**Выводы:**
- Sliding — самый дешёвый, но теряет факты.
- Sticky Facts — лучший баланс, если правила извлечения фактов адекватные.
- Branching незаменим для "что если" сценариев и A/B тестинга промптов.

**Код:** `challenge/days/day-10-strategies/index.ts`

---

## День 11 — <заглушка>
**Дата:** TBD

**Задание:**
> TBD

**Сделано:**
- (заполняется при получении задания)

**Выводы:**
- TBD

**Код:** `challenge/days/day-11-name/` (ветка `day-11`)
