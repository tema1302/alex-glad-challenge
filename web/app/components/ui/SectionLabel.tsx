export function SectionLabel({ children }: { children: string }) {
  return (
    <div role="heading" aria-level={2} className="mb-3 font-mono text-xs uppercase tracking-wider text-dim">// {children}</div>
  );
}
