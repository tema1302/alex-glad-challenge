// Контент публичной страницы /demo «Спроси мою базу знаний» — RAG-демо по
// публично-безопасному корпусу (руководство пользователя EVOLUTE i-SPACE,
// партиция `structure` rag.sqlite). Только данные: без Tailwind-классов
// (классы — в app/demo/*), без секретов, без импортов lib/server и @challenge
// (замороженный контракт П-3: client-остров знает только §2 API и эти данные).

export const demoMeta = {
  title: 'Спроси мою базу знаний',
  description:
    'Живое RAG-демо без логина: задайте вопрос по руководству EVOLUTE i-SPACE — эмбеддинг-поиск по базе ' +
    'знаний, ответ LLM со списком источников и честное «не знаю», если релевантного фрагмента в базе нет.',
};

export const demoLead = {
  label: 'demo · rag',
  headline: 'Спроси мою базу знаний',
  subhead:
    'Задайте вопрос — система найдёт релевантные фрагменты в собранной базе знаний (руководство ' +
    'пользователя автомобиля EVOLUTE i-SPACE) и ответит со ссылками на источники. Чего в базе нет, ' +
    'модель не придумает: сработает guard, и вы получите честное «не знаю».',
};

// «Как это устроено» — короткий блок под формой (retrieval → guard → LLM).
export const demoHow = {
  title: 'Как это устроено',
  body:
    'Вопрос превращается в эмбеддинг и ищется по базе знаний — 553 фрагмента руководства EVOLUTE ' +
    'i-SPACE. Лучшие совпадения попадают в контекст LLM, ответ собирается с отсылками вида [1]–[4] ' +
    'к источникам. Если релевантного фрагмента не нашлось, guard обрывает запрос до генерации: ' +
    'вместо выдумки — честное «не знаю».',
};

// Чипы «спросить про пример»: три вопроса из реальных текстов корпуса
// (guard не сработает) + один намеренно вне базы — демонстрация честного отказа.
export interface DemoExample {
  question: string;
  /** Подпись на чипе для нестандартных примеров (например, «вне базы»). */
  hint?: string;
}

export const demoExamples: readonly DemoExample[] = [
  { question: 'Как открыть крышку багажного отсека?' },
  { question: 'Как включить подогрев передних сидений?' },
  { question: 'Почему запрещено самостоятельно разбирать высоковольтную батарею?' },
  { question: 'Кто выиграл чемпионат мира по футболу?', hint: 'вне базы' },
];

// Тексты формы и результатов. Ошибки API приходят из роута (safeMessage) —
// здесь человекочитаемые фолбэки для сети/таймаута клиента.
export const demoCopy = {
  questionLabel: 'Ваш вопрос',
  questionPlaceholder: 'Например: как открыть крышку багажного отсека?',
  ask: 'Спросить',
  asking: 'Отвечаю… обычно 10–30 секунд',
  emptyHint: 'Выберите пример или введите свой вопрос — до 300 символов.',
  answerTitle: 'Ответ',
  sourcesTitle: 'Источники',
  sourcesHint: '[n] — номер отсылки в тексте ответа',
  notKnowTitle: 'Честное «не знаю»',
  notKnowNote:
    'Релевантного фрагмента в базе нет — модель не стала ничего выдумывать. ' +
    'Так работает guard RAG-конвейера: лучше отказ, чем галлюцинация.',
  errorNetwork: 'Не получилось связаться с сервером. Проверьте сеть и попробуйте ещё раз.',
  errorTimeout:
    'Ответ не пришёл за 60 секунд — база знаний или модель перегружены. Попробуйте ещё раз чуть позже.',
  errorBaseUnavailable: 'База знаний временно недоступна. Попробуйте позже — или разборы уже в канале.',
} as const;

// Нижний CTA. Сама кнопка — SubscribeButton (единственный конверсионный
// элемент, как на лендинге); url/label берутся из data/landing.ts channel.
export const demoCta = {
  title: 'Полные разборы — в канале',
  desc:
    'Разборы каждой подсистемы — RAG-движок, эмбеддинги, guard «не знаю», цена локальных моделей — ' +
    'выходят в Telegram-канале: с кодом, цифрами и граблями.',
} as const;

// --- Замороженный контракт POST /api/demo/rag (план §2) — типы client-острова ---

/** Источник в ответе 200. Полей source/chunkId нет намеренно (privacy-by-default). */
export interface DemoRagSource {
  n: number;
  title: string;
  section: string;
  snippet: string;
  score: number;
}

export type DemoRagResponse =
  | { ok: true; answer: string; gaveUp: boolean; sources: DemoRagSource[] }
  | { ok: false; error: string; retryAfterSec?: number };
