// Клиент-safe пояснения FSM конвейера блог-агентов (meetup-web) — по аналогии с
// уже передаваемым сервером мапом `labels`. Без импортов core/: тип стадии — из
// lib/shared/forms (pipelineStageSchema), тексты статичны. FSM в core заморожен;
// при изменении стадий эта карта зайдёт в рассинхрон — сознательный риск,
// задокументирован в плане (комментарий здесь + typecheck-гейт).
import type { PipelineStageInput } from './forms';

/** Что делает стадия + примеры входа/выхода человеческим языком. */
export interface PipelineStageExplainer {
  /** Что происходит на стадии (для карты под текущей стадией). */
  what: string;
  /** Пример входа стадии. */
  inputExample: string;
  /** Пример выхода стадии. */
  outputExample: string;
}

/** Человеческое интро страницы /blog/pipeline (замена разработческому абзацу). */
export const PIPELINE_INTRO = {
  headline: 'Конвейер блог-агентов',
  text:
    'Четыре агента превращают RSS-новость в готовый пост для Telegram-канала: ' +
    'агент-разведчик выбирает тему → агент-автор пишет черновик → агент-фактчекер проверяет цифры → ' +
    'агент-редактор доводит текст. Ниже — живой автомат этого конвейера: нажимайте разрешённые ' +
    'переходы и смотрите, что происходит на каждом шаге.',
};

export const PIPELINE_STAGE_EXPLAINER: Record<PipelineStageInput, PipelineStageExplainer> = {
  idle: {
    what: 'Конвейер стоит и ждёт запуска.',
    inputExample: 'входа нет — это стартовая точка',
    outputExample: 'запуск → агент-разведчик сканирует источники новостей',
  },
  planning: {
    what: 'Агент-разведчик ранжирует свежие новости и выбирает тему.',
    inputExample: '~20 заголовков из RSS-фидов за последние часы',
    outputExample: 'одна выбранная новость + обоснование выбора',
  },
  execution: {
    what: 'Агент-автор пишет черновик поста по стилю канала.',
    inputExample: 'текст выбранной новости + образцы стиля канала',
    outputExample: 'черновик поста (~400–700 символов)',
  },
  validation: {
    what: 'Агент-фактчекер проверяет утверждения и цифры черновика по источникам.',
    inputExample: 'черновик + полные тексты источников',
    outputExample: 'вердикт: подтверждено / опровергнуто / требует уточнения',
  },
  revision: {
    what: 'Агент-редактор вносит правки после фактчекинга.',
    inputExample: 'черновик + вердикт фактчекера',
    outputExample: 'финальный текст поста, готовый к публикации',
  },
  done: {
    what: 'Пост готов — конвейер завершён.',
    inputExample: 'финальный текст из редактуры',
    outputExample: 'готовый пост; далее публикация в канал или сброс конвейера',
  },
};
